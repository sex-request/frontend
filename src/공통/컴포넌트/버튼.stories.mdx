import { Meta, Story, Canvas } from '@storybook/addon-docs';

import 버튼 from 'src/공통/컴포넌트/버튼.style';

<Meta
  title="공통/컴포넌트/버튼"
  component={버튼}
/>

export const Template = (args) => (<버튼 {...args}>{args.text}</버튼>);

<Canvas>
  <Story
    name="샘플"
    args={{ text: '버튼' }}
    argTypes={{ onClick: { action: 'clicked' } }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
