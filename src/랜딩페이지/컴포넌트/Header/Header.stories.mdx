import { Meta, Story, Canvas } from '@storybook/addon-docs';

import Header from 'src/랜딩페이지/컴포넌트/Header';

<Meta
  title="랜딩페이지/컴포넌트/Header"
  component={Header}
/>

export const Template = (args) => (<Header {...args} />);

<Canvas>
  <Story
    name="헤더"
  >
    {Template.bind({})}
  </Story>
</Canvas>
