import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { useState } from 'react';

import 이름입력란 from './이름입력란';

<Meta
  title="랜딩페이지/컴포넌트/이름입력란"
  component={이름입력란}
/>

export const Template = (args) => {
  const [value, setValue] = useState('');
  return (
    <이름입력란
      {...args}
      value={value}
      onChange={setValue}
    />
  );
};

<Canvas>
  <Story
    name="text"
  >
    {Template.bind({})}
  </Story>
</Canvas>
