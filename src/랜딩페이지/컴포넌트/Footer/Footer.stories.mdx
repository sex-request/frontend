import { Meta, Story, Canvas } from '@storybook/addon-docs';

import Footer from 'src/랜딩페이지/컴포넌트/Footer';

<Meta
  title="랜딩페이지/컴포넌트/Footer"
  component={Footer}
/>

export const Template = (args) => (<Footer {...args} />);

<Canvas>
  <Story
    name="푸터"
  >
    {Template.bind({})}
  </Story>
</Canvas>
